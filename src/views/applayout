<template>
  <div class="min-h-screen bg-gradient-to-br from-orange-50 via-pink-50 to-purple-50">
    <!-- Header -->
    <header class="sticky top-0 z-50 backdrop-blur-lg bg-white/80 border-b border-purple-100 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Logo -->
          <router-link 
            to="/"
            class="flex items-center space-x-2 group cursor-pointer"
          >
            <div class="text-3xl transform group-hover:scale-110 transition-transform duration-300">
              🐾
            </div>
            <h1 class="text-2xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
              SmartPetShop
            </h1>
          </router-link>

          <!-- Desktop Navigation -->
          <nav class="hidden md:flex items-center space-x-8">
            <router-link 
              v-for="link in navLinks" 
              :key="link.path"
              :to="link.path"
              class="text-gray-700 hover:text-purple-600 transition-colors duration-200 font-medium"
              active-class="text-purple-600"
            >
              {{ link.name }}
            </router-link>
          </nav>

          <!-- Right Icons -->
          <div class="flex items-center space-x-4">
            <!-- Search -->
            <button 
              @click="showSearch = !showSearch"
              class="p-2 rounded-full hover:bg-purple-100 transition-colors duration-200 relative"
            >
              <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
              </svg>
            </button>

            <!-- Favorites -->
            <button 
              @click="$router.push('/favorites')"
              class="p-2 rounded-full hover:bg-purple-100 transition-colors duration-200 relative"
            >
              <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
              </svg>
              <span 
                v-if="favoritesCount > 0"
                class="absolute -top-1 -right-1 bg-pink-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center"
              >
                {{ favoritesCount }}
              </span>
            </button>

            <!-- Cart -->
            <button 
              @click="$router.push('/cart')"
              class="p-2 rounded-full hover:bg-purple-100 transition-colors duration-200 relative"
            >
              <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"/>
              </svg>
              <span 
                v-if="cartCount > 0"
                class="absolute -top-1 -right-1 bg-purple-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center"
              >
                {{ cartCount }}
              </span>
            </button>

            <!-- User -->
            <button 
              @click="handleUserClick"
              class="p-2 rounded-full hover:bg-purple-100 transition-colors duration-200"
            >
              <svg class="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
              </svg>
            </button>
            
            <!-- Mobile Menu Button -->
            <button 
              @click="menuOpen = !menuOpen"
              class="md:hidden p-2 rounded-full hover:bg-purple-100 transition-colors duration-200"
            >
              <svg v-if="!menuOpen" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
              </svg>
              <svg v-else class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <transition name="slide-down">
        <div v-if="menuOpen" class="md:hidden bg-white border-t border-purple-100 shadow-lg">
          <nav class="px-4 py-4 space-y-2">
            <router-link
              v-for="link in navLinks"
              :key="link.path"
              :to="link.path"
              @click="menuOpen = false"
              class="block px-4 py-2 rounded-lg hover:bg-purple-50 text-gray-700 font-medium"
              active-class="bg-purple-100 text-purple-600"
            >
              {{ link.name }}
            </router-link>
          </nav>
        </div>
      </transition>

      <!-- Search Overlay -->
      <transition name="fade">
        <div v-if="showSearch" class="absolute top-full left-0 right-0 bg-white border-b border-purple-100 shadow-lg p-4">
          <div class="max-w-3xl mx-auto">
            <input
              v-model="searchQuery"
              type="text"
              placeholder="Search for pets, food, supplies..."
              class="w-full px-6 py-3 rounded-full border-2 border-purple-200 focus:border-purple-600 focus:outline-none"
              @keyup.enter="handleSearch"
            />
          </div>
        </div>
      </transition>
    </header>
